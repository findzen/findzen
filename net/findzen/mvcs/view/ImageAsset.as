package net.findzen.mvcs.view{    import com.greensock.events.LoaderEvent;    import com.greensock.loading.ImageLoader;    import com.greensock.loading.LoaderMax;    import com.greensock.loading.LoaderStatus;    import com.greensock.loading.SWFLoader;    import flash.display.MovieClip;    import net.findzen.utils.Log;    import net.findzen.mvcs.view.base.AssetBase;    public class ImageAsset extends AssetBase    {        public function ImageAsset($container:MovieClip)        {            super($container);        }        override public function set url($url:String):void        {            this.autoLoad = true;            _loader = new ImageLoader($url, { name: this.name, container: this.container });            super.url = $url;        }    }}
package app.signals
{
    import org.osflash.signals.Signal;

    public class ViewStateChange extends Signal
    {
        public function ViewStateChange()
        {
            super(Array);
        }
    }
}

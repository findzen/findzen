package app.signals
{
    import org.osflash.signals.Signal;
    import org.osflash.signals.ISignal;

    public class PlaceCall extends Signal
    {
        public function PlaceCall()
        {
            super(ISignal);
        }
    }
}

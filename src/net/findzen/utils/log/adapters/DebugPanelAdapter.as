package net.findzen.utils.log.adapters{    import net.findzen.utils.DebugPanel;    public class DebugPanelAdapter implements ILogAdapter    {        public function DebugPanelAdapter()        {        }        public function output($prefix:String, $level:String, ... $objects):void        {            var output:String = $prefix;            for(var k:String in $objects)                output += ' ' + ($objects[k] ? $objects[k].toString() : null);            DebugPanel.output($level, output);        }        public function clear():void        {        }    }}